import 'package:flutter/material.dart';

class IntroductionUI extends StatelessWidget {
  const IntroductionUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('IntroductionPage'),);
  }
}
