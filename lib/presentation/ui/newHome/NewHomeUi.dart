import 'package:flutter/material.dart';

class NewHomeUi extends StatelessWidget {
  const NewHomeUi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
