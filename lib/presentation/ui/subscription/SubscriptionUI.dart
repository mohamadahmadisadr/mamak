import 'package:flutter/material.dart';

class SubscriptionUI extends StatelessWidget {
  const SubscriptionUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('SubscriptionUI'),);
  }
}
