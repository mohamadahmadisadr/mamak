import 'package:flutter/material.dart';

class EstimateUI extends StatelessWidget {
  const EstimateUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('EstimateUI'),);
  }
}
